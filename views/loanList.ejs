<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loan List</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            text-align: left;
            padding: 8px;
            border-bottom: 1px solid #ddd;
        }
        tr:hover {
            background-color: #f5f5f5;
        }
        thead {
            background-color: #ffa07a;
        }
        h1 {
            text-align: center;
        }
    </style>
</head>
    <body>
        <h1>Loan Lists || EJS View</h1>
        <table>
            <thead>
                <tr>
                    <th>Customer ID</th>
                    <th>Customer Name</th>
                    <th>Phone Number</th>
                    <th>Address</th>
                    <th>Loan Amount</th>
                    <th>Interest</th>
                    <th>Loan Term Years</th>
                    <th>Loan Type</th>
                    <th>Description</th>
                    <th>Created Date</th>
                </tr>
            </thead>
            <tbody>
                <!-- Loop through loans data and populate the table rows -->
                <% for (let i = 0; i < loans.length; i++) { %>
                    <tr>
                        <td><%= loans[i].id %></td>
                        <td><%= loans[i].customerName %></td>
                        <td><%= loans[i].phoneNumber %></td>
                        <td><%= loans[i].address %></td>
                        <td><%= loans[i].loanAmount %></td>
                        <td><%= loans[i].interest %></td>
                        <td><%= loans[i].loanTermYears %></td>
                        <td><%= loans[i].loanType %></td>
                        <td><%= loans[i].description %></td>
                        <td><%= loans[i].createdDate %></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </body>
</html>